---
description: Reset optloop working state for the current repository
disable-model-invocation: true
---

Run exactly:

```bash
bash "${CLAUDE_PLUGIN_ROOT}/bin/optloop" reset
```

Only report the result.
