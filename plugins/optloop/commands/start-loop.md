---
description: Start the optloop supervisor in the background
disable-model-invocation: true
---

Run exactly:

```bash
bash "${CLAUDE_PLUGIN_ROOT}/bin/optloop-launch"
```

Only report whether launch succeeded.
Do not inspect repository files automatically.
Do not attempt benchmark diagnosis in the foreground session.
